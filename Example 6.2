
;Replace the number in AX by its absolute value

.model small
.stack 100h
.data
.code

main proc
    
    mov ax,'-5'
    cmp ax,0        ;if ax<0
    
    JNL Endif ;jump if not less then or wqual to
    neg ax
    
    
    Endif:
    
    
    mov dx,ax
    mov ah,2
    int 21h
    
    
    mov ah,4ch
    int 21h
    
    
main endp
END main
